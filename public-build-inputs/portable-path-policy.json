{
  "schemaVersion": "ckb-portable-path-policy-v1",
  "baselineState": "historicalPrePortableBaseline",
  "unmappedAction": "fail",
  "sourceRootRuleId": "source-lock-repository-commit-tree-relative-v1",
  "allowlist": ["/usr/bin/env", "/dev/null"],
  "placeholders": [
    "{{CKB_PATH:PROJECT_ROOT}}",
    "{{CKB_PATH:SOURCE_ROOT}}",
    "{{CKB_PATH:USER_HOME}}",
    "{{CKB_PATH:SDK_ROOT}}",
    "{{CKB_PATH:INSTALL_ROOT}}",
    "{{CKB_PATH:BUILD_ROOT}}",
    "{{CKB_PATH:TMPDIR}}",
    "{{CKB_PATH:ABSOLUTE}}"
  ],
  "fieldCoverage": [
    "sourceId", "sourceUrl", "repo", "path", "derivedFrom",
    "title", "content", "chunk", "citation"
  ],
  "categories": [
    {"id": "source_locator", "action": "source_lock_relative"},
    {"id": "declared_project_root", "action": "placeholder"},
    {"id": "user_home", "action": "placeholder"},
    {"id": "sdk_install_build_tmp", "action": "explicit_root_placeholder"},
    {"id": "runtime_state", "action": "exclude"},
    {"id": "non_filesystem_slash_text", "action": "preserve"},
    {"id": "absolute_path_semantics_example", "action": "semantic_placeholder"},
    {"id": "unmapped", "action": "fail"}
  ]
}
